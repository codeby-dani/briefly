# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets. `*.local` above already covers .env.local; these are explicit so a
# stray .env is never one typo away from a public repo.
.env
.env.*
!.env.example

# Netlify CLI
.netlify

# Netlify CLI scratch (netlify dev writes this for its edge runtime)
deno.lock
